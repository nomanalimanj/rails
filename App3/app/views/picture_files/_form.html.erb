<%= form_with(model: picture_file, local: true) do |form| %>
  <% if picture_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture_file.errors.count, "error") %> prohibited this picture_file from being saved:</h2>

      <ul>
      <% picture_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
